# Novel Engine Configuration

# Embedding model for RAG (optional, falls back to BM25 if not set)
EMBED_BASE_URL=
EMBED_MODEL=Qwen/Qwen3-Embedding-8B
EMBED_API_KEY=

# Reranker model for RAG (optional)
RERANK_BASE_URL=
RERANK_MODEL=jina-reranker-v3
RERANK_API_KEY=

# Project workspace root (default: current directory)
WORKSPACE_ROOT=
